from django.apps import AppConfig


class ServicesConfig(AppConfig):
    name = 'Services'
    #Renombramos el nombre de la app por defecto con lo siguente verbose
    verbose_name="Gestor de servicios"
